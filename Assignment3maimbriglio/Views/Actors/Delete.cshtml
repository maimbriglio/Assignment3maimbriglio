@model Assignment3maimbriglio.Models.Actor

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>@Html.DisplayFor(model => model.name)</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            Name
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.name)
        </dd>
        <dt class = "col-sm-2">
            Gender
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.gender)
        </dd>
        <dt class = "col-sm-2">
            Age
        </dt>
        <dd class = "col-sm-10">c
            @Html.DisplayFor(model => model.age)
        </dd>
        <dt class = "col-sm-2">
            IMDB
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.IMDB)
        </dd>
        <dt class="col-sm-2">
            Photo
        </dt>
            <dd class="col-sm-10">
                @if (@Model.photo != null)
                {
                        <img width="200" height="300" src="@Url.Action("GetActorPhoto", "Actors", new {@id = @Model.id})" />
                }
            </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
